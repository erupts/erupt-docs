# View（视图注解）

> 占位文档，内容待补充。

`@View` 注解配置字段在列表表格中的列展示行为。

## 核心属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `title` | String | 列标题 |
| `width` | String | 列宽度 |
| `show` | boolean | 是否显示，默认 true |
| `type` | ViewType | 展示类型（图片、链接等）|
