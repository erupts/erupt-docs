# erupt-web

> 占位文档，内容待补充。

`erupt-web` 模块包含 Erupt 管理系统的前端静态资源，基于 Angular 构建，以 Jar 形式分发，无需单独部署前端。

```xml
<dependency>
    <groupId>xyz.erupt</groupId>
    <artifactId>erupt-web</artifactId>
</dependency>
```
